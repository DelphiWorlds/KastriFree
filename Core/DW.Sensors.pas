unit DW.Sensors;

interface

uses
  System.Sensors;

type
  TLocationUsage = (Unauthorized, WhenInUse, Always);
  TLocationActivity = (Other, Automotive, Fitness, Navigation);

implementation

end.
